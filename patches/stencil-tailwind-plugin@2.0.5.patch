diff --git a/dist/index.js b/dist/index.js
index 2fe62dfad23acb2b05ca285391b21cecbb7cc37f..03cc6e8896c6bf86a93e099c8ea17460d495c695 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -423,6 +423,10 @@ async function transformStyleStatement(opts, sourceFile, sourceText, filename) {
     cssNode2.text = reducedClasses;
   };
   const cssNode = walkTo(sourceFile, stringLiteralPath);
+  if (!cssNode) {
+    debug("[Stylesheets]", "CSS already in arrow function format, skipping:", filename);
+    return sourceText;
+  }
   await stringStyleRewriter(cssNode);
   const printer = import_typescript2.default.createPrinter();
   return printer.printFile(sourceFile);
diff --git a/dist/index.mjs b/dist/index.mjs
index ff2cee1a97ae9b4e8a7ff84dfb1a7199452d7226..04038044fa356209fb93e584f45a8be10e9294e5 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -387,6 +387,10 @@ async function transformStyleStatement(opts, sourceFile, sourceText, filename) {
     cssNode2.text = reducedClasses;
   };
   const cssNode = walkTo(sourceFile, stringLiteralPath);
+  if (!cssNode) {
+    debug("[Stylesheets]", "CSS already in arrow function format, skipping:", filename);
+    return sourceText;
+  }
   await stringStyleRewriter(cssNode);
   const printer = ts2.createPrinter();
   return printer.printFile(sourceFile);
