<script lang="ts">
  // Root page redirects to /home via +page.ts load function.
  // This component only renders if redirect fails.
  const _placeholder = true;
</script>

<svelte:head> <title>Redirecting…</title> </svelte:head>
